# Changelog

Все заметные изменения в этом проекте будут задокументированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/spec/v2.0.0.html).



## [1.2.1] — 2026-05-12

### Исправлено
- При ошибке капчи пост больше не теряется: поля формы сохраняются, показывается предупреждение на английском "Invalid captcha code".

## [1.2.0] — 2026-05-12

### Добавлено
- Полная интернационализация (i18n): поддержка русского, английского, немецкого и французского языков.
- Глобальный переключатель языка в админке — список языков строится автоматически по JSON-файлам в `translations/`.
- Функция `t()` встроена в Jinja-шаблоны и доступна во всех view через `services.i18n`.
- Локализованы все пользовательские и административные шаблоны, flash-сообщения, `abort()`-сообщения и формы.
- Структура переводов на основе JSON (`translations/{ru,en,de,fr}.json`).
- Добавлен `Content-Security-Policy` заголовок для усиления защиты от XSS.
- Автоматическая очистка таблицы `post_fts` при удалении постов и тредов.

### Изменено
- Улучшено кеширование статики (CSS/JS — год, медиа — месяц) для ускорения загрузки в I2P/Tor.
- Обновлён `install.sh`: используется единый `run_migrations(app)`, дефолтные настройки синхронизированы с `Settings.DEFAULTS`.
- Pre-commit обновлён: оставлены только `black`, `isort` и grep-проверка на `config.get`.
- Удалён `ruff`, чтобы избежать ложных срабатываний и сохранить контроль над форматированием.

### Исправлено
- Ошибка `UNIQUE constraint failed: post_fts.post_id` при создании новых постов после удаления старых.
- Несоответствие CSRF-токенов в админке (все формы теперь используют правильные `action`).
- Множество мелких непереведённых строк в пагинации, каталоге, поиске, форме создания треда, странице закрытой доски и кнопке «Наверх».
- Исправления синтаксиса и отступов после массовых автоматических замен.

### Безопасность
- Устранены все известные уязвимости в зависимостях (зафиксированы минимальные безопасные версии).
- Включён тройной анализ в CI/CD: `bandit`, `CodeQL`, `pip-audit` (все в строгом режиме).
- Добавлен `SECURITY.md` с PGP-ключом и описанием процесса ответственного разглашения.

## [1.1.0] — 2026-05-11

### Добавлено
- Класс `Settings` (`core/settings.py`) для централизованного управления всеми настройками с правильной типизацией (bool/int/list). Все настройки загружаются из БД и кешируются.
- Полный переход всех модулей на `Settings`: удалены все разрозненные вызовы `current_app.config.get(...)`.
- Pre-commit хуки: `black`, `isort`, кастомная проверка на оставшиеся `config.get`.
- CI/CD (GitHub Actions):
  - `bandit` для статического анализа безопасности (строгий режим, severity >= medium).
  - `pip-audit` для проверки уязвимостей в зависимостях.
  - `CodeQL` для глубокого анализа кода.
  - Автоматическая проверка отсутствия прямых вызовов `config.get`.
- Dependabot сгруппирован по категориям (flask, sqlalchemy, security, other).
- Файл `CHANGELOG.md`.
- Обновлённый `install.sh`, использующий `run_migrations()` и `Settings.DEFAULTS` для согласованности.

### Изменено
- `core/settings.py`: дефолтные значения настроек (`DEFAULTS`) синхронизированы с `install.sh`.
- `conftest.py`: тестовые настройки явно переопределяются через `app.config` для изоляции от `Settings._cache`.
- `blueprints/admin.py`, `blueprints/board.py`, `blueprints/radio.py`, `blueprints/main.py`, `core/middleware.py`, `services/media.py`: все чтения настроек заменены на `current_app.config["SETTINGS"].<свойство>`.
- Удалены неиспользуемые файлы: `core/config.py`, скрипты `migrate_admin_to_settings.sh`, `migrate_board_to_settings.sh`.
- `requirements.txt` обновлён: зафиксированы минимальные безопасные версии пакетов (Flask 3.1.3, Pillow 12.1.1, Werkzeug 3.1.6, requests 2.33.0, waitress 3.0.1, python-dotenv 1.2.2).

### Исправлено
- Ошибка 500 на главной странице, вызванная отсутствием таблицы `board` после тестов.
- Проблема с `IndentationError` в `app.py` при некорректной вставке `setup_logging`.
- Тесты больше не зависят от состояния БД `setting` и корректно отключают капчу через `app.config`.

### Безопасность
- Устранены известные уязвимости в зависимостях (CVE в Flask, Pillow, Werkzeug, requests, waitress, python-dotenv).
- Включён многоуровневый анализ: bandit (паттерны кода), CodeQL (глубокий анализ), pip-audit (зависимости).

## [1.0.2] — 2026-04-21
- Первоначальный публичный релиз
